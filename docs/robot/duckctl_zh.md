# `duckctl`：电脑端命令

`duckctl` 让电脑在没有 Wi-Fi 和 SSH 的情况下通过蓝牙访问机器人，是手机应用的命令行
替代品。它负责发现、配对、读取信息并调用机器人允许的 RPC。

## 使用边界

- 电脑端客户端不随机器人 daemon 一起部署。
- 蓝牙链路不代表请求天然可信，机器人仍需执行身份和权限校验。
- 网络变化或 BLE 断线时，命令应返回明确错误，而不是假装成功。
- 涉及电机和更新的操作应遵守机器人端同样的安全门禁。

英文源文档：[duckctl.md](duckctl.md)。
