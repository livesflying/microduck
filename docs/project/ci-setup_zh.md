# CI 配置

本文记录发布流水线的一次性配置，包括签名密钥、GitHub secrets、staging 和 stable
发布流程，以及密钥轮换。

## 核心原则

- 发布签名在 CI 中完成，不在开发者电脑上生成正式签名。
- staging 用于安装到开发板和候选验证。
- stable 发布应复用已经验证过的构建产物。
- 发布密钥和开发密钥分离，开发构建不能安装到客户机器人。
- 密钥轮换必须同时更新 CI、信任锚和恢复流程。

具体 secret 名称、权限和命令以源文档为准。

英文源文档：[ci-setup.md](ci-setup.md)。
