# 安装路径缺少测试

本文记录安装路径中曾经连续出现的缺陷，以及为什么原有单元测试和 `board-test.sh`
没有捕获它们。该问题已关闭，但经验已经沉淀到更新系统设计中。

## 经验

- 首次安装对开发板做的每项系统修改，也必须在每次更新的 hook 中保持一致。
- 安装脚本、systemd 配置、驱动、目录权限和信任密钥需要端到端测试。
- “二进制构建成功”不等于“新版本能在真实板上启动”。
- 健康门禁和回滚测试必须覆盖安装失败、重启中断和依赖缺失。

英文源文档：[install-path-gap.md](install-path-gap.md)。
