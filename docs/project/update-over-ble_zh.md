# 从手机通过蓝牙更新机器人

本文记录通过 BLE 启动和观察更新流程的验证。`btd` 转发允许的更新方法，
`updaterd` 返回进度，而客户端通过 `call` 访问这些方法。

## 设计要求

- 蓝牙断开不应让正在执行的更新进入不一致状态。
- 更新进度应保存在机器人端，客户端重连后可以继续查询。
- 启动更新、查询状态、取消或失败都必须返回明确结果。
- 签名、哈希、健康检查和回滚规则与 SSH 或本地更新完全一致。

英文源文档：[update-over-ble.md](update-over-ble.md)。
